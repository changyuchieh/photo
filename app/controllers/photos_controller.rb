class PhotosController < ApplicationController
    # def serve
    #     @picture = Picture.find_by(params[:id])
    #     # send_data(@picture.album, :type => @picture.mime_type, :filename => "#{@picture.name}.png", :disposition => "inline")
    # end

end
